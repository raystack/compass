---
title: Installation
description: Install Compass using pre-compiled binaries, Docker, or build from source.
order: 3
---

# Installation

## Binary

Download the appropriate version for your platform from the [releases](https://github.com/raystack/compass/releases) page.

### macOS

```sh
brew install raystack/tap/compass
```

To upgrade:

```sh
brew upgrade compass
```

### Linux

Download the `.deb` or `.rpm` from the [releases](https://github.com/raystack/compass/releases/latest) page:

```sh
sudo dpkg -i compass_*.deb    # Debian/Ubuntu
sudo rpm -i compass_*.rpm     # RHEL/CentOS
```

### Windows

```sh
scoop bucket add compass https://github.com/raystack/scoop-bucket.git
```

## Docker

```sh
docker pull raystack/compass:latest
```

Shell alias for running Compass via Docker:

```sh
mkdir -p $HOME/.config/raystack
alias compass="docker run -e HOME=/tmp -v $HOME/.config/raystack:/tmp/.config/raystack --user $(id -u):$(id -g) --rm -it -p 8080:8080/tcp raystack/compass:latest"
```

## Build from Source

```bash
git clone https://github.com/raystack/compass.git
cd compass
make
```

This creates the `compass` binary in the project root.

## Verify

```bash
compass --help
```

## Next Steps

- [Configuration](./configuration) -- Set up the server and database
- [Quickstart](./quickstart) -- Get running with sample data
